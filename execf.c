#include <stdlib.h>

int main(){
  /*
  just test for executing commands when adding in
  variable PATH.

  of course, this is just for testing
  scripts in bash is more easy and practical
  */
  system("sudo apt update");

  return 0;
}